run (for starting the blockchain and setup the hardhat) 
`./scripts/run_system.sh`

---
run (for starting the frontend) 
`cd frontend && npm run dev `

---
default ENS values : vishal.eth, test.eth

---
Use the MetaMask Extension for wallets and connect the localhost HardHat version to this metamask to view and preview transactions

---
All the approvals and rejections are reflected in the backend terminal
